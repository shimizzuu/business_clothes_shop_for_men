$('#productsList').html("<%= escape_javascript(render partial: 'products_list',
                                                locals: { 
                                                  products: @products,
                                                  taxon: @taxon }) %>");
